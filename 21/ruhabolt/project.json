{
  "name": "ruhabolt",
  "$schema": "node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "./src",
  "projectType": "application",
  "targets": {
    "build": {
      "executor": "@nx/vite:build",
      "outputs": ["{options.outputPath}"],
      "defaultConfiguration": "production",
      "options": {
        "outputPath": "dist/ruhabolt"
      },
      "configurations": {
        "development": {
          "mode": "development"
        },
        "production": {
          "mode": "production"
        }
      }
    },
    "serve": {
      "executor": "@nx/vite:dev-server",
      "defaultConfiguration": "development",
      "options": {
        "buildTarget": "ruhabolt:build"
      },
      "configurations": {
        "development": {
          "buildTarget": "ruhabolt:build:development",
          "hmr": true
        },
        "production": {
          "buildTarget": "ruhabolt:build:production",
          "hmr": false
        }
      }
    },
    "preview": {
      "executor": "@nx/vite:preview-server",
      "defaultConfiguration": "development",
      "options": {
        "buildTarget": "ruhabolt:build"
      },
      "configurations": {
        "development": {
          "buildTarget": "ruhabolt:build:development"
        },
        "production": {
          "buildTarget": "ruhabolt:build:production"
        }
      }
    },
    "test": {
      "executor": "@nx/vite:test",
      "outputs": ["coverage/ruhabolt"],
      "options": {
        "passWithNoTests": true,
        "reportsDirectory": "coverage/ruhabolt"
      }
    },
    "lint": {
      "executor": "@nx/linter:eslint",
      "outputs": ["{options.outputFile}"],
      "options": {
        "lintFilePatterns": ["./src/**/*.{ts,tsx,js,jsx}"]
      }
    },
    "serve-static": {
      "executor": "@nx/web:file-server",
      "options": {
        "buildTarget": "ruhabolt:build"
      }
    }
  },
  "tags": []
}
